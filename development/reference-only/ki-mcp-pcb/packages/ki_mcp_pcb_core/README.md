# ki-mcp-pcb-core

Reusable library — Canonical Intermediate Representation (CIR), parsers, synthesis, placement, routing, validation, export.

This is the package the CLI and MCP server are built on top of. If a feature touches PCB design semantics, it belongs here.

See the root [`SPEC.md`](../../SPEC.md) for architectural rules.
