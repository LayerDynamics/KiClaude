---
description: Validate a CIR YAML spec.
argument-hint: <path to .yaml>
---

Call the `pcb_validate_cir` MCP tool with the absolute path:

$ARGUMENTS

Render the report as a brief table (severity / code / where / message). If there are errors, suggest the minimum edit to fix the first one. Do not auto-edit.
